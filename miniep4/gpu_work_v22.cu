#include "utils.h"

__global__
void gpu_work_v2(double *arr)
{
        // The GPU kernel
}

// Launch the work on arr and return it at results;
void launch_gpu_work_v2(double *arr, double **results)
{
        // Launch the GPU kernel
}
